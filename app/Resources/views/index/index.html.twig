{% extends 'base.html.twig' %}

{% block content %}
    {{ form_start(form) }}
    {# render the task's only field: description #}
    {{ form_row(form.email) }}

    <h3>Billets</h3>
    <ul class="billets" data-prototype="{{ form_widget(form.billets.vars.prototype)|e('html_attr') }}">
        {% for billet in form.billets %}
            <li class="list-unstyled">{{ form_widget(form) }}</li>
        {% endfor %}

    </ul>
    <button type="submit" class="btn btn-default">valider</button>

    {{ form_end(form) }}
    <li class="list-unstyled">{{ user.email }}</li>
    <li class="list-unstyled">{{ user.id }}</li>


{% endblock %}

{% block javascripts %}
   {{ parent() }}

{% endblock %}