<nav class="navbar navbar-default">
    <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('home') }}">Le Louvre</a>
        <ol class=" text-center breadcrumb">
            {% if app.request.get('_route') == 'home'%}
            <li class="disabled">Billeterie</li>
            {% elseif app.request.get('_route') == 'recapitulatif' %}
            <li><a href="{{ path('home') }}">Billeterie</a></li>
            <li class="disabled">Récapitulatif</li>
            {% endif %}
        </ol>
        </div>
    </div><!-- /.container-fluid -->
</nav>